# Copy this file to .env.local and fill in values
# Never commit .env.local to version control

# Neon Postgres connection string (server-side only, never expose to client)
DATABASE_URL=postgres://...

# Optional: Auth.js secret (openssl rand -base64 32)
# AUTH_SECRET=
